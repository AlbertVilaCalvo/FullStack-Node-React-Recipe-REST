name: CI - Kubernetes

on:
  pull_request:
    branches:
      - main
    paths:
      - kubernetes/**
      - .github/workflows/ci-kubernetes.yml
  push:
    branches:
      - main
    paths:
      - kubernetes/**
      - .github/workflows/ci-kubernetes.yml
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  kubeconform:
    name: Kubeconform
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
      - name: Install Kustomize
        run: |
          KUSTOMIZE_VERSION=5.8.1
          curl -sSL "https://github.com/kubernetes-sigs/kustomize/releases/download/kustomize%2Fv${KUSTOMIZE_VERSION}/kustomize_v${KUSTOMIZE_VERSION}_linux_amd64.tar.gz" \
            | tar -xz -C /usr/local/bin kustomize
          kustomize version
      - name: Install Kubeconform
        run: |
          KUBECONFORM_VERSION=0.7.0
          curl -sSL "https://github.com/yannh/kubeconform/releases/download/v${KUBECONFORM_VERSION}/kubeconform-linux-amd64.tar.gz" \
            | tar -xz -C /usr/local/bin kubeconform
          kubeconform -v
      - name: Validate manifests (dev)
        # -strict: fail on unknown fields in known schemas
        # -ignore-missing-schemas: skip (don't fail) CRDs without a known schema, e.g. ExternalSecret, SecretStore
        # -summary: print a summary line at the end
        run: kustomize build kubernetes/server/overlays/dev | kubeconform -strict -ignore-missing-schemas -summary -
      - name: Validate manifests (prod)
        run: kustomize build kubernetes/server/overlays/prod | kubeconform -strict -ignore-missing-schemas -summary -

  kube_linter:
    name: KubeLinter
    runs-on: ubuntu-24.04
    continue-on-error: true
    steps:
      - uses: actions/checkout@v6
      - name: Install Kustomize
        run: |
          KUSTOMIZE_VERSION=5.8.1
          curl -sSL "https://github.com/kubernetes-sigs/kustomize/releases/download/kustomize%2Fv${KUSTOMIZE_VERSION}/kustomize_v${KUSTOMIZE_VERSION}_linux_amd64.tar.gz" \
            | tar -xz -C /usr/local/bin kustomize
          kustomize version
      - name: Install KubeLinter
        run: |
          KUBE_LINTER_VERSION=0.7.2
          curl -sSL "https://github.com/stackrox/kube-linter/releases/download/v${KUBE_LINTER_VERSION}/kube-linter-linux.tar.gz" \
            | tar -xz -C /usr/local/bin
          kube-linter version
      - name: Run KubeLinter - server (dev)
        continue-on-error: true
        run: kustomize build kubernetes/server/overlays/dev | kube-linter lint -
      - name: Run KubeLinter - server (prod)
        continue-on-error: true
        run: kustomize build kubernetes/server/overlays/prod | kube-linter lint -
      - name: Run KubeLinter - argocd-apps (dev)
        continue-on-error: true
        run: kube-linter lint kubernetes/argocd-apps/dev/
      - name: Run KubeLinter - argocd-apps (prod)
        continue-on-error: true
        run: kube-linter lint kubernetes/argocd-apps/prod/
      - name: Run KubeLinter - karpenter-nodepool (dev)
        continue-on-error: true
        run: kube-linter lint kubernetes/karpenter-nodepool/dev/
      - name: Run KubeLinter - karpenter-nodepool (prod)
        continue-on-error: true
        run: kube-linter lint kubernetes/karpenter-nodepool/prod/
