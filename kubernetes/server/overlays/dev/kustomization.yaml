apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

# Labels for all resources
labels:
  - pairs:
      environment: dev

patches:
  - path: ingress_patch.yaml
